<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns the actual theme; this is just the splash/window background -->
    <style name="Theme.WeekendOut" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
